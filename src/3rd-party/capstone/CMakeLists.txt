#
# Ugly. Is there a better way?
#

set(BUILD_DIET OFF CACHE BOOL "")
set(BUILD_STATIC ON CACHE BOOL "")
set(BUILD_SHARED OFF CACHE BOOL "")
set(BUILD_TESTS OFF CACHE BOOL "")
set(ARM_SUPPORT ON CACHE BOOL "")
set(ARM64_SUPPORT OFF CACHE BOOL "")
set(MIPS_SUPPORT OFF CACHE BOOL "")
set(PPC_SUPPORT OFF CACHE BOOL "")
set(SPARC_SUPPORT OFF CACHE BOOL "")
set(SYSZ_SUPPORT OFF CACHE BOOL "")
set(XCORE_SUPPORT OFF CACHE BOOL "")
set(X86_SUPPORT OFF CACHE BOOL "")
set(X86_REDUCE OFF CACHE BOOL "")

if(${CMAKE_CXX_COMPILER_ID} MATCHES "GNU")
    add_definitions(-Wno-unused-parameter)
endif()

if(${CMAKE_CXX_COMPILER_ID} MATCHES "Clang")
    add_definitions(-Wno-unused-parameter)
endif()

add_subdirectory(capstone)

unset(BUILD_DIET CACHE)
unset(BUILD_STATIC CACHE)
unset(BUILD_SHARED CACHE)
unset(BUILD_TESTS CACHE)
unset(ARM_SUPPORT CACHE)
unset(ARM64_SUPPORT CACHE)
unset(MIPS_SUPPORT CACHE)
unset(PPC_SUPPORT CACHE)
unset(SPARC_SUPPORT CACHE)
unset(SYSZ_SUPPORT CACHE)
unset(XCORE_SUPPORT CACHE)
unset(X86_SUPPORT CACHE)
unset(X86_REDUCE CACHE)
unset(USE_DEFAULT_ALLOC CACHE)

# vim:set et sts=4 sw=4 nospell:
